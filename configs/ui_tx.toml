# T114 UI transmitter — DIN MIDI in, 902–928 MHz.
app       = "t114_ui"
board     = "t114"
role      = "tx"
tx_source = "uart"
diversity = false
band_plans = ["ism915", "sennheiser", "shure", "dense_lo", "dense_mid", "dense_hi", "wide"]
keys      = "osrf-keys.toml"
