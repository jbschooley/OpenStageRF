# T114 UI transmitter — DIN MIDI in, 470–514 MHz (CN470/SX1268).
app       = "t114_ui"
board     = "t114"
role      = "tx"
tx_source = "uart"
diversity = false
band_plans = ["band470", "shure_g58_*", "senn_a1_*"]
keys      = "osrf-keys.toml"
